The following commands may be necessary to get everything working correctly:

```
git submodule update --init --recursive # to get tbb
pip install torch # to get torch
pip install ninja # to get C++ support
```
